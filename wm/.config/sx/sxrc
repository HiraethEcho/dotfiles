# 设置 X 显示环境变量
export DISPLAY=:0

# 将环境变量导入 systemd 用户会话
systemctl --user import-environment DISPLAY

# 启动 graphical-session.target
systemctl --user start graphical-session.target

# 启动您的窗口管理器（dwm）
exec dwm
